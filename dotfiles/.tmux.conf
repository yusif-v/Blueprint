# --- General ---

set -g mouse on
set -g history-limit 10000
set -g base-index 1
setw -g pane-base-index 1

# --- Prefix ---
unbind C-b
set -g prefix C-a
bind C-a send-prefix

# --- Splits ---
bind | split-window -h
bind - split-window -v
unbind '"'
unbind %
